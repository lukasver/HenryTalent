export enum USER_ROLES {
  Admin = 'admin',
  Creator = 'creator',
  Selector = 'selector',
}

export enum SKILLS {
  Tech = 'tech',
  Soft = 'soft',
  Other = 'other',
}

export enum VISIBILITY {
  Unlisted = 'unlisted',
  Listed = 'listed',
  Private = 'private',
}

export enum STATUS {
  Unemployed = 'unemployed',
  Employed = 'employed',
}

export enum FOLDER_STATUS {
  Draft = 'draft',
  Created = 'created',
  Sent = 'sent',
}
